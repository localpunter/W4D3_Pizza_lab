<h2>All the orders!</h2>

<ul>
  <% @orders.each do |order| %>
    <p>Name: <%= order.pretty_name() %></p>
    <p>Order: <%= order.quantity %> x <%= order.topping %></p>
    <p>Cost: £<%= order.quantity * order.price %></p>
    <p><a href="/pizza-orders/<%= order.id %>">Show Order</a></p>
    <hr/>
  <% end %>
</ul>
